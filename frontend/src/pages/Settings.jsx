import React from "react";
import AppShell from "../components/AppShell.jsx";
import { useAuth } from "../context/AuthContext.jsx";
import LanguageSelect from "../components/LanguageSelect.jsx";
export default function Settings(){const {user}=useAuth();return <AppShell><div className="mx-auto w-full max-w-[1400px] px-5 py-8 sm:px-6 lg:py-10"><div className="mb-8"><p className="eyebrow">Account</p><h1 className="mt-2 font-display text-4xl font-semibold">Settings</h1><p className="mt-3 text-sm leading-6 text-slate">Your account and learning preferences.</p></div><div className="dashboard-card divide-y divide-ink/10 overflow-hidden"><div className="p-6"><p className="text-xs font-mono uppercase tracking-wider text-slate">Profile</p><p className="mt-2 text-lg font-semibold">{user?.name || "Student"}</p><p className="mt-1 text-sm text-slate">{user?.email || "Email not available"} · {user?.studentClass || "Class not set"}</p></div><div className="p-6"><p className="text-xs font-mono uppercase tracking-wider text-slate">Learning goal</p><p className="mt-2 text-sm font-medium">{user?.aim || "Not set"}</p></div><div className="p-6 flex items-center justify-between gap-5"><div><p className="text-xs font-mono uppercase tracking-wider text-slate">Interface language</p><p className="mt-2 text-sm text-slate">Translate the complete Trailhead interface.</p></div><LanguageSelect /></div><div className="bg-paper p-6"><p className="text-xs leading-6 text-slate">More profile, notification, and learning-preference controls can be connected here as the backend settings API is added.</p></div></div></div></AppShell>}
